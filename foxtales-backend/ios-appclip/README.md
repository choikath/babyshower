# FoxTales App Clip — moved

The original loose-file scaffold that lived here has been built out into a real,
buildable Xcode project at [`../ios/`](../ios/), with the player logic refactored
into a shared module (`FoxTalesCore`) that both the full app and the App Clip
depend on.

- **Build & run, configuration, App Store Connect setup:** [`../ios/README.md`](../ios/README.md)
- **The load-bearing background-audio spike:** [`../ios/SPIKE.md`](../ios/SPIKE.md)
- **Product/architecture narrative:** [`../docs/appclip-experience.md`](../docs/appclip-experience.md)

This directory is kept only as a redirect; everything else moved to `../ios/`.
